# Descargas anteriores del backend

Diez descargas del Google Sheets entre el 6 y el 7 de agosto de 2026, en el orden en que las
numeró el navegador. Las citan las actas `ACTA-001` a `ACTA-004` como el archivo contra el que se
verificó cada cierre; se conservan por eso y no se editan.

**Ninguna es la hoja vigente ni el entregable.** Los archivos vivos están en `BD/`:
`Modelo_Datos_09082026.xlsx` es la descarga de producción y `Modelo_Datos_PLANTILLA.xlsx` es el
entregable, que se genera con `python scripts/generar_plantilla.py`.
